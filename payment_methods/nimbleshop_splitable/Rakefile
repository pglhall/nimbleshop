# encoding: UTF-8
require 'bundler/gem_tasks'
require 'rake/testtask'
require 'rdoc/task'

desc 'Default: run tests'
task :default => ['nimbleshop_splitable:test']
task :test => ['nimbleshop_splitable:test']

namespace :nimbleshop_splitable do

  desc 'Run nimbleshop_splitable tests.'
  Rake::TestTask.new(:test) do |t|
    t.libs << 'lib'
    t.libs << 'test'
    t.pattern = 'test/**/*_test.rb'
    t.verbose = true
  end

end

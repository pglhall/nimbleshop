<%= form_tag @splitable_record.splitable_submission_url, method: 'post', id: 'post_to_splitable', style: 'display:none;' do %>

  <% @base_data_for_splitable.each do |key, value| %>
    <%= text_field_tag(key, value) %>
  <% end %>

  <% @line_items_data_for_splitable.each do |key, value| %>
    <%= text_field_tag(key, value) %>
  <% end %>

  <%= submit_tag %>
<% end %>

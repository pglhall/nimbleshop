<% unless current_order.shipping_address.new_record? %>
  <div class='row'>
    <div class='span4' style='min-height:150px;'>
      <h3>
        Shipping address
        <small>
          <%= link_to 'Edit', new_order_shipping_address_path(current_order), title: 'Edit Shipping Address', id: 'edit_shipping_address' %>
        </small>
      </h3>
      <address class='shipping-address'>
        <%= display_address(current_order.shipping_address) %>
      </address>

      <h3> Billing address </h3>

      <address class='billing-address'>
      <% if current_order.shipping_address.use_for_billing %>
        <i>Same as shipping address</i>
      <% else %>
        <%= display_address(current_order.billing_address) %>
      <% end %>
      </address>

      <h3>Contact email</h3>
      <address>
        <%= current_order.email %>
      </address>
    </div>
  </div>
<% end %>

<div class='span10'>

  <%= render 'products/breadcrumb' %>

  <% products.in_groups_of(3) do |group| %>
     <ul class="unstyled row">
        <% group.each do |product| %>
            <% if product.blank? %>
              &nbsp;
            <% else %>
              <li class="span3">
              <%= render 'products/product', product: product %>
              </li>
            <% end %>
        <% end %>
     </ul>
  <% end %>
</div>
<!--single produst width 204*258-->
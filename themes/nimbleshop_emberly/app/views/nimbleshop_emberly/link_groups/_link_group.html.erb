<div class="sidebar_block">
  <h3><%= link_group.name %></h3>

  <ul class="unstyled">
    <% link_group.navigations.each do |nav| %>
    	<li> <%= link_to nav.product_group.name, nav.product_group, title: nav.product_group.name %> </li>
    <% end %>
  </ul>
</div>
